# Same-origin proxy via Next.js (recommended — one ngrok tunnel on :3000)
NEXT_PUBLIC_BACKEND_URL=/api/backend

# Or direct backend URL if you expose :8000 with a second ngrok tunnel:
# NEXT_PUBLIC_BACKEND_URL=https://YOUR-BACKEND-TUNNEL.ngrok-free.dev
